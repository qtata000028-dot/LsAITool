# 任务待办

## 2026-03-22 明细页签与内部视图职责重整

### Requirement Spec
- 目标：重新梳理基础档案“明细页签”和“明细内部视图（表格/图表/网页等）”的配置职责，去掉重复配置，并让图表详情改成通过点击图表预留区进入。
- 影响范围：`src/components/Dashboard.tsx` 中 `detail-tab`、`detail-grid`、图表预览区点击逻辑，以及图表右侧检查器布局。
- 关键约束：
  - 明细页签只保留页签级属性，不再重复承载内部表格/图表已有的配置。
  - 表格点击后进入明细整表同构配置；图表点击后进入图表专属配置。
  - 图表右侧详情要按 `p_systemdlltabchart` 继续保留字段语义，但布局要更干净、分组更清晰。
  - 图表配置入口要通过画布里的图表视图预留区触发，而不是只靠底部类型切换。
  - 继续沿用当前 shadcn/workbench 语法，不回退旧 WinForm 样式。
- 不做什么：
  - 不改限制措施页。
  - 不新增新的明细填充模式。
  - 不回退当前模块继承主表配置 / SQL 构列能力。

### Checklist
- [ ] 更新 `tasks/lessons.md`，记录“明细页签和内部视图不能重复配置”的教训。
- [ ] 阅读 `detail-tab / detail-grid / 图表预留区` 的点击与选中链路。
- [ ] 收敛明细页签右侧，只保留页签级属性。
- [ ] 让图表点击预留区进入图表详情，并重构图表右侧布局。
- [ ] 运行 `npm run lint` 并浏览器验证表格/图表点击切换详情正确、控制台无错误。

## 2026-03-22 明细检查器布局重构

### Requirement Spec
- 目标：重构基础档案下“明细模块”和“明细表格”右侧检查器，解决当前信息碎、层级乱、多个小卡片和入口互相打架的问题，让填写路径更顺、视觉更干净。
- 影响范围：`src/components/Dashboard.tsx` 中 `detail-tab` 与 `detail-grid` 两个右侧检查器分支，以及它们共用的数据来源/扩展入口布局。
- 关键约束：
  - 继续沿用当前 shadcn/workbench 语法，不回退旧 WinForm 样式表格。
  - 明细模块与明细表格要统一骨架，但职责仍然清晰分开。
  - 右侧不再堆叠低价值摘要卡、说明文案和过多的嵌套入口。
  - 高频填写项优先直给，低频配置保留清晰入口，但不能继续做成一屏碎卡片。
  - 不能破坏上一轮“模块编号继承主表配置 / SQL 构列 / 图表配置对齐文档”的语义。
- 不做什么：
  - 不改限制措施页。
  - 不回退当前明细数据来源逻辑。
  - 不新增新的明细配置模式。

### Checklist
- [x] 更新 `tasks/lessons.md`，记录“右侧检查器不能堆碎卡片”的教训。
- [x] 阅读 `detail-tab` 与 `detail-grid` 当前检查器实现，识别乱点和重复信息。
- [x] 重构明细模块右侧检查器，收敛成更清晰的分组表单。
- [x] 重构明细表格右侧检查器，简化数据来源与扩展配置入口布局。
- [x] 运行 `npm run lint` 并用浏览器验证明细模块与明细表格两条链路无报错、排版更顺。

### Progress Summary
- 已确认当前乱点主要有两类：一是 `detail-tab` 把定义、来源、统计和入口混在一屏碎卡片里；二是 `detail-grid` 的数据来源区叠加了过多小卡片、摘要块和入口按钮，导致填写路径被打断。
- 已将 `detail-tab` 的右侧检查器收成三大组：`明细列表`、`数据来源与关联`、`开关与权限`。原来的多个小统计卡被合并到“当前明细”和“常用入口”两个摘要块里，主填写路径保持单列更顺。
- 已将 `detail-grid` 的右侧检查器收成两块主结构：`数据来源` 和 `明细表属性`。数据来源区改成“左侧填写 / 下方来源摘要与入口”单列结构，不再横向挤压；表格属性也从 3 张信息小卡改成一个摘要块。
- 为了适配当前非全屏右侧宽度，已把相关表单从硬编码 `sm:grid-cols-2` 改成了自适应或单列工作流，避免标签和输入框被挤成竖排。

### Verification
- `npm run lint`：通过。
- 浏览器验证：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录`。
  - 验证 1：点击 `关联附件`，右侧切到 `明细模块 · 关联附件`，能看到整理后的 `明细列表 / 数据来源与关联 / 开关与权限` 三组布局。
  - 验证 2：点击明细画布，右侧切到 `明细表配置 · 关联附件`，`数据来源` 与 `明细表属性` 两块排版更顺，配置入口不再被多层卡片分散。
  - 验证 3：控制台 `error = 0`。
- 截图：
  - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-tab-inspector-layout-cleanup-v2.png`
  - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-inspector-layout-cleanup.png`

### Result Notes
- 这次的关键不是再“美化一个卡片”，而是把右侧检查器的层级重新梳理成更少的容器和更短的填写路径。对这种高频工作台，减少碎信息和嵌套入口比继续加说明更重要。
- 当前仍保留了模块继承、SQL 构列、图表配置和显示字段/颜色/右键这些原有能力，但入口已经被收进更统一的 shadcn/workbench 填写节奏里，后面再继续 polish 时不用重来一遍结构。

## 2026-03-22 明细图表配置对齐 p_systemdlltabchart

### Requirement Spec
- 目标：纠正当前“明细图表”右侧详情只显示落点提示、没有真正可编辑配置的问题，按 `朗速详细设计平台V2-表结构设计文档(1).docx` 中 `p_systemdlltabchart` 的字段口径重建图表配置面板。
- 影响范围：`src/components/Dashboard.tsx` 中基础档案 `detail-grid` 在 `图表` 填充方式下的右侧检查器、图表配置状态与布局。
- 关键约束：
  - 图表配置属于明细整表配置的一部分，不能再停留在“图表配置表 = p_systemdlltabchart”的映射卡层级。
  - 继续沿用当前主表/明细整表统一后的 shadcn/workbench 语法，不引入一套新的旧式表格配置 UI。
  - 右侧说明文字继续最小化，优先保证填写顺手。
  - 字段口径以 `p_systemdlltabchart` 为准，优先覆盖用户截图中的核心项：图表标题、图表类型、图表颜色、3D、X/Y 轴字段与标题、Y 轴共享、禁用、Y 轴字段2/3、禁显数字。
  - 其余扩展字段只保留必要入口，不堆大段解释。
- 不做什么：
  - 不回退上一轮 detail-tab / detail-grid 的职责拆分。
  - 不改限制措施页。
  - 不把图表配置挪出当前明细整表检查器。

### Checklist
- [x] 更新 `tasks/lessons.md`，记录“指定历史表结构时不能只留映射卡”的教训。
- [x] 从 DOCX 抽取 `p_systemdlltabchart` 字段口径并映射到前端配置模型。
- [x] 将当前 `detail-grid` 在图表模式下的右侧详情重构为完整图表配置卡组，而不是简单落点提示。
- [x] 保持数据来源、明细表属性、字段标识与图表配置之间的工作台节奏统一。
- [x] 运行 `npm run lint`，并在浏览器中验证切换到“图表”后右侧显示完整图表配置且无控制台错误。

### Progress Summary
- 已确认当前实现只有 `renderDetailChartTableMappingSection`，仍停留在“图表配置表 = p_systemdlltabchart”的提示层，未提供真实编辑能力。
- 已从 `朗速详细设计平台V2-表结构设计文档(1).docx` 中抽出 `p_systemdlltabchart` 字段，当前核心字段包括：`chartType / chartTitle / chartColor / chart3D / XLabelField / YValueField / XAxisTitle / YAxisTitle / YAxisShared / isVisible / yvaluefield1 / yvaluefield2 / valueVisible`，并有 `markVisible / legendVisible / labelangle / labelvisible / labelsize / labelSpaced / circlejagge / circlehollow` 等扩展项。
- 已补 `chartConfig` 数据模型并并入 `buildGridConfig`，使图表配置和明细整表配置共用同一条状态链，不再额外维护孤立分支。
- 已将图表模式下的右侧详情改为 `图表设置 + 数据来源 + 图表加载属性 + 字段标识` 的工作台结构，图表设置里直接可编辑标题、图表类型、颜色、X/Y 轴字段与标题、第二/第三指标、3D、Y 轴共享、禁用、禁显数字等字段。
- 已修正模块编号继承逻辑，切换来源模块时会保留当前图表配置，不会被主表整表配置整体覆盖掉。

### Verification
- `npm run lint`：通过。
- Playwright 浏览器验证：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> 点击 `关联附件` 明细画布 -> 点击底部 `图表`。
  - 结果 1：右侧标题切到 `明细图表配置 · 关联附件`，并显示 `p_systemdlltabchart` 标识。
  - 结果 2：右侧出现完整图表配置表单，可编辑 `图表标题 / 图表类型 / 图表颜色 / 备用颜色 / X轴字段 / X轴标题 / Y轴字段 / Y轴标题 / Y轴字段2 / Y轴字段3`。
  - 结果 3：`3D / Y轴共享 / 禁用 / 禁显数字 / 标签禁显 / 标题禁显 / 显示网格线 / 禁显标签 / 绝对值轴 / 锯齿圆 / 空心圆` 开关可正常切换。
  - 结果 4：实际填写 `附件上传趋势 / attachment_name / uploader / 附件 / 上传人`，状态即时更新，`Y轴共享` 可正常勾选。
- 控制台：`browser_console_messages(level=\"error\")` 返回 `0` 条错误。
- 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-chart-p_systemdlltabchart.png`

### Result Notes
- 这次的关键不是再加一张“图表配置表”提示卡，而是把 `p_systemdlltabchart` 真正并入明细整表配置模型；用户切到图表时，右侧现在看到的是可编辑配置，而不是技术映射说明。
- 图表配置继续挂在 `detail-grid` 这条主链上，因此数据来源、按 SQL 构列、字段标识翻译和图表字段配置不会互相断开，后续维护也只需要围绕一套明细整表工作台继续扩展。
- 当前图表类型选项按高频办公场景先提供了 `柱形图 / 折线图 / 圆饼图 / 条形图 / 面积图` 五种；如果后面文档或旧系统还有更细的枚举，可在这套 `chartConfig` 骨架上继续补，不需要再重构状态链。

## 2026-03-22 基础档案明细配置纠偏与主表对齐

### Requirement Spec
- 目标：纠正上一轮把“明细模块详情”和“明细整表配置”拆成两套语义的问题，按用户给的新截图把基础档案下的明细配置重新对齐到主表配置模型。
- 目标：明细表格配置和主表保持同一套语法与结构，只在数据来源上增加两条规则：
  - 配了模块编号：直接对照/继承对应模块的主表配置。
  - 配了 SQL：按 SQL 自动构建列名，其余配置仍沿用主表语法。
- 影响范围：`src/components/Dashboard.tsx` 中 `detail-tab` / `detail-grid` 的配置模型、右侧检查器、明细整表数据源逻辑与布局。
- 关键约束：
  - 不再保留“明细模块一套、明细整表一套”两条平行配置链路。
  - 明细整表右侧必须和主表整表配置保持一致的主从结构、页签节奏和填写方式。
  - 模块编号不是另一种 UI 模式，只是 SQL/列默认值的继承入口。
  - SQL 生成列名后，列配置、颜色、右键、图表等仍按主表整表配置继续工作。
  - 右侧说明文字继续最小化，优先保证高频填写顺手。
- 不做什么：
  - 不新增第三种明细来源模式。
  - 不改限制措施页。
  - 不改基础档案主表配置语义。

### Checklist
- [x] 更新 `tasks/lessons.md`，记录“明细模块和明细整表不能拆成两套配置模型”的教训。
- [x] 重写 `detail-tab` 的右侧详情，使其对齐用户截图里的明细列表配置字段，而不是继续沿用 `p_systemdlltabdetail`。
- [x] 重构 `detail-grid` 的数据源区域，收敛成一套主表同构配置：模块编号继承主表配置、SQL 自动构列。
- [x] 让明细整表继续复用主表的显示字段 / 颜色 / 右键 / 图表配置结构，排版收成更紧凑的 shadcn/workbench 风格。
- [x] 运行 `npm run lint`，并在浏览器里验证基础档案下点击明细模块、模块编号继承、SQL 构列和切换链路无错误。

### Progress Summary
- 已确认上一轮误解点：`detail-tab` 被错误改成了 `p_systemdlltabdetail` 风格，而 `detail-grid` 又单独承担了数据源和整表配置，导致同一个明细需要在两处理解。
- 已确认当前代码已有可复用的主表整表配置骨架、SQL 构列能力和模块候选摘要，适合直接收口成“主表同构 + 明细来源增强”的模型。
- 已将 `detail-tab` 收成“明细列表定义”，字段口径对齐用户给的旧 WinForm 截图：列表名称、类型、DLL 模板、关联模块、关联字段、关联值、关联条件、开关项、权限与备注等都归到这一处。
- 已将 `detail-grid` 收成与主表整表一致的工作台语法：表格属性、字段标识、颜色规则、右键菜单仍走主表同构结构，只把“数据来源”单独增强为模块编号继承或 SQL 构列。
- 已移除明细整表右侧旧的双模式心智和重复 SQL 入口，当前点击明细模块与点击明细整表分别只承担“列表定义”和“整表配置”各自职责。

### Verification
- `npm run lint`：通过。
- Playwright 浏览器验证：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> 点击 `关联附件` -> 点击明细画布。
  - 验证 1：点击 `关联附件` 后，右侧切到 `明细模块 · 关联附件`，显示列表名称、类型、DLL 模板、关联模块、关联字段、关联值、关联条件、开关项与权限。
  - 验证 2：点击明细画布后，右侧切到 `明细表配置 · 关联附件`，显示 `数据来源 / 明细表属性 / 字段标识` 三块，且不再出现旧的双模式冗余入口。
  - 验证 3：点击模块快捷项 `FM-CO-001` 后，页面提示“已继承 FM-CO-001 的主表配置”，明细 SQL 切换为 `SELECT * FROM customer_archive`，明细列同步为 5 列。
  - 验证 4：清空模块编号、把 SQL 改成 `SELECT attachment_name, uploader, created_at FROM customer_attachment` 后点击 `按 SQL 构列`，页面提示“已按 SQL 同步 3 个字段”，明细列同步为 `attachment_name / uploader / created_at`。
- 控制台：`browser_console_messages(level=\"error\")` 返回 `0` 条错误。
- 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-grid-main-table-aligned.png`

### Result Notes
- 这次的关键不是继续给明细单独造一套“模块模式 / SQL 模式”界面，而是把明细整表重新拉回主表同构配置，只把“来源”做成增强入口；这样显示字段、颜色、右键、图表的认知路径才不会断开。
- `detail-tab` 和 `detail-grid` 的职责已经重新分开：前者管“明细列表定义”，后者管“明细整表配置”。后续再扩展字段时，只需要判断它属于“页签定义”还是“整表配置”，不需要再新开第三条支线。
- 当前仓库里只有当前模块的完整主表配置在内存中，其他模块只有模块编号与主 SQL 摘要；因此“模块编号继承”对当前模块是完整继承，对其他模块是“沿用主表同构骨架 + 对应模块主 SQL/列”。如果后面要做到真正跨模块完整继承，需要补一份跨模块主表配置源。

## 当前任务

### 需求规格
- 目标：重构配置向导第 6 步“限制措施”，把原来的表格查验 + 大块表单改成更适合重度配置的卡片式主从工作台。
- 影响范围：`限制措施` 的工作台布局、规则列表呈现方式、详情编辑区结构，以及 `管控限制措施` 页签的主要交互。
- 关键约束：
  - 不再坚持表格隐喻，优先保证同屏能看到多条规则摘要。
  - 顶部只保留模块名，不再展示编码、表名、统计大卡。
  - 左侧用于快速查验和切换，右侧用于多卡片编辑，视觉上保持统一。
  - 长脚本和长文本继续支持通过“详情编辑”弹层处理。
  - 不能改动现有限制措施的数据结构和字段语义。
- 不做内容：
  - 不新增新的业务字段。
  - 不改动其他步骤的配置逻辑。

### 执行清单
- [x] 结合最新用户反馈确认问题已从“看不到详情”转为“界面仍像表格页、留白过多、重度配置不顺手”。
- [x] 更新 `tasks/lessons.md`，记录不要继续执着保留表格隐喻。
- [x] 将第 6 步重构为左侧规则卡片列表 + 右侧详情工作台。
- [x] 将 `管控限制措施` 的详情区改成“摘要条 + 多张紧凑配置卡”。
- [x] 保留长文本弹层编辑，同时压缩默认详情区的空白。
- [x] 运行类型检查并按真实浏览器窗口验证布局和交互。
- [x] 将只读的申请时间、申请人员移出右侧配置区，改到左侧卡片做查验显示。
- [x] 移除限制措施中的 `基础表单元素`、`单据抬头元素`、`单据明细元素` 三个页签。
- [x] 统一重构 `编号规则管理`、`顶层数据结构`、`流程设计管理` 的右侧布局，消除大面积留白。
- [x] 优化左侧卡片中的时间、创建人查验样式。

### 进度摘要
- 限制措施页已放弃表格查验区，左侧改成规则卡片列表，可直接查看多条规则的摘要信息。
- `管控限制措施` 右侧已从大块表单改为摘要条、基础定义卡、状态卡、备注卡、脚本卡的组合式布局。
- 当前默认屏内可以同时看到更多摘要信息，底部不再是大面积纯留白。
- 只读字段不再混入右侧可编辑区域，申请时间和申请人员已回收到左侧卡片底部。
- 限制措施现在只保留真正可编辑的四个业务页签，其余三个元素页签已从第 6 步移除。
- `编号规则管理`、`顶层数据结构`、`流程设计管理` 已统一成摘要条 + 多卡片的工作台布局。

### 验证记录
- `npm run lint`：通过。
- Playwright 浏览器验证：
  - 视口：`1600x900`
  - 路径：登录 -> 打开 `成本控制模块 A` 配置 -> 跳转第 6 步 `限制措施`
  - 结果：左侧显示规则卡片列表，右侧显示摘要条 + 多张配置卡；无新的脚本错误。
- 控制台：仅存在已有的 `favicon.ico 404`，没有新的运行时错误。
- 截图：`C:/Users/24485/AppData/Local/Temp/playwright-mcp-output/1774056611431/page-2026-03-21T02-07-32-094Z.png`
- 二次验证：
  - 路径：登录 -> 打开 `成本控制模块 A` 配置 -> 第 6 步 `限制措施` -> 切换 `编号规则管理 / 顶层数据结构 / 流程设计管理`
  - 结果：元素类页签已消失；剩余三页签都切成卡片工作台；控制台错误为 0。
  - 截图：`C:/Users/24485/AppData/Local/Temp/playwright-mcp-output/1774056611431/page-2026-03-21T02-56-11-875Z.png`

### 结果复盘
- 这类重度配置界面不适合继续沿用“表格预览 + 下方大表单”的旧结构，继续压高度只会让界面更别扭。
- 左侧摘要卡 + 右侧多卡片编辑更适合频繁切换和高密度配置，也更接近用户当前想要的工作台体验。
- 本轮继续证明“结构可用”不等于“界面有秩序”，限制措施四个页签还需要统一收敛视觉噪音，避免一页一个节奏。
- 不能编辑的字段必须坚持停留在左侧摘要区；右侧只保留真正需要操作的配置，避免用户在配置区里看到重复信息。
- 详情卡里同一个字段只展示一次，能在摘要条里编辑的内容不应再在下方表单里重复出现，否则会直接制造混乱感。

## 2026-03-21 明细数据源双模式整合

### Requirement Spec
- 目标：把明细表格的数据源配置整合成两种模式，`明细 SQL` 和 `模块引用`，避免页签行为和表格数据源分散在两处配置。
- 目标：让员工在配置明细时，能快速看清当前明细到底是“自己写 SQL”还是“引用别的模块主表”，切换和定位都要直接。
- 影响范围：`src/components/Dashboard.tsx` 里的明细页签配置、明细表格配置、明细列翻译入口，以及对应检查器面板。
- 关键约束：
  - `明细页签` 只保留页签行为，不再承担真正的数据源编辑。
  - `明细表格` 的通用配置里必须出现统一的数据源模式切换入口。
  - `SQL` 模式继续保留 SQL 配置与列标识翻译。
  - `模块引用` 模式要支持配置模块编号、关联条件，并给出可用模块摘要。
  - 若引用当前模块主表，应支持一键把主表字段带入明细列，减少重复配置。
- 不做内容：
  - 不改现有落表语义。
  - 不新增第三种明细数据源。

### Checklist
- [ ] 更新 lessons，记录“互斥的数据源策略必须统一进一个入口”的规则。
- [ ] 给明细表格配置补上 `sourceMode / sourceModuleCode / sourceCondition` 的完整编辑体验。
- [ ] 将明细页签里的“关联模块 / 关联条件”改成只读摘要与跳转入口。
- [ ] 在 `模块引用` 模式下支持当前模块主表字段一键带入明细列。
- [ ] 跑 `npm run lint` 并做浏览器验证，确认模式切换、列同步和右侧配置无卡顿。

### Progress Summary
- 已确认根因：当前 `detailTabConfig` 和 `detailTableConfig` 分别持有一半数据源配置，导致用户无法在一个地方完整理解明细来源。
- 已为明细表格配置补上 `sourceMode / sourceModuleCode / sourceCondition` 字段，并将明细页签改成只读摘要入口。
- 已把明细整表配置重构成统一的数据源卡，`明细 SQL` 与 `模块引用` 共用一套入口，避免在页签行为和表格属性之间来回找。
- `模块引用` 模式已支持模块编号快捷选择、关联条件长文本编辑，以及“带入当前模块主表列”的同步入口。

### Verification
- `npm run lint`：通过
- Playwright：
  - 视口：默认桌面窗口
  - 路径：登录 -> 成本控制模块 A -> 配置向导第 5 步 -> 点击 `关联附件` 明细表画布 -> 切换 `模块引用` -> 选择 `FM-CO-001` -> 点击 `带入当前模块主表列`
  - 结果：
    - `明细 SQL` 模式默认显示 SQL 状态、默认条件、列翻译摘要
    - 切到 `模块引用` 后显示模块编号、关联条件、引用模块摘要
    - 选择当前模块后，按钮可用，点击后明细列由 2 列切换为当前主表的 5 列
    - 控制台 `error` 为 `0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-source-dual-mode-workbench.png`

### Result Notes
- 这类互斥数据源配置不适合拆到“页签行为 + 整表属性”两边，统一入口后用户才能在同一视线里理解当前明细到底怎么取数。
- `模块引用` 模式当前先把“编号、条件、摘要、当前模块带列”打通，能满足高频配置场景；跨模块字段自动映射后续可以在这个骨架上继续补。

## 2026-03-21 模块设置舞台留白修正

### Requirement Spec
- 目标：修正第 5 步 `模块设置` 在单表 / 明细场景下的底部大留白，不再出现内容缩在上半屏、下半屏整片发空的状态。
- 影响范围：`src/components/Dashboard.tsx` 中模块设置主舞台高度策略、单表默认上下 pane 分配，以及明细工作区的实际占高。
- 关键约束：
  - 不能只压缩右侧配置卡片，必须从外层舞台高度与上下 pane 分配上修正。
  - 需要保留上下拖拽能力，但默认比例要更适合高频配置。
  - 第 5 步全屏模式下优先“铺满可用高度”，而不是使用固定 900+ 高度。
- 不做内容：
  - 不改明细数据源双模式语义。
  - 不改单表字段和检查器的业务逻辑。

### Checklist
- [ ] 更新 lessons，记录“留白常见根因在舞台高度策略而不是右侧卡片”的规则。
- [ ] 调整模块设置第 5 步全屏舞台高度为自适应可用空间。
- [ ] 调整单表默认上下 pane 高度，使下方明细工作区默认更可用。
- [ ] 跑 `npm run lint` 并按单表明细场景做浏览器验证。

### Progress Summary
- 已确认根因：第 5 步 `document` 工作台仍在使用固定近整屏高度，外层内容区没有真正把剩余空间吃满，导致底部空白被整体留在舞台下方。

### Verification
- 待补充

### Result Notes
- 待补充

## 后续任务模板

### 需求规格
- 目标：
- 影响范围：
- 关键约束：
- 不做内容：

### 执行清单
- [ ] 待补充

### 进度摘要
- 待补充

### 验证记录
- 待补充

### 结果复盘
- 待补充
# 2026-03-21 高频配置体验优化

## Requirement Spec
- 目标：优化配置向导中模块设置的高频操作手感，让明细页签切换、列点击和整表点击更直接，不出现明显延迟或“卡手”。
- 目标：收掉“基础档案明细”工作区点击后的大面积空白，让下方明细区更紧凑、更像可工作的画布。
- 影响范围：`src/components/Dashboard.tsx` 中的明细工作台、表格构建器、选中态计算链路，以及 fullscreen 工作区高度策略。
- 关键约束：
  - 不能牺牲现有右侧配置逻辑和字段映射。
  - 明细页签、列选中、整表选中必须保持语义一致。
  - 视觉优化必须服务于高频配置效率，而不是只做装饰。
- 不做内容：
  - 不新增业务字段。
  - 不改限制措施页的数据结构。

## Checklist
- [x] 移除明细与检查器链路中的延迟选中值，确保点击立即反馈。
- [x] 压缩基础档案明细区的空态与表格画布高度，消除大块无效留白。
- [x] 收敛 fullscreen 工作区高度，避免因固定最小高度导致整体被拉空。
- [x] 运行 `npm run lint` 并按接近用户真实窗口的尺寸做浏览器验证。

## Progress Summary
- 明细页签和列点击已改为立即切换，去掉了检查器上下文里的延迟值。
- 明细工作区把填充方式并入顶部工具条，表格画布改成紧凑密度，空态区不再用整块高度硬撑。
- fullscreen 工作区重新按视口高度铺满，`基础档案明细` 场景下 stage 到底部操作栏的间隙已收敛到约 `36px`。

## Verification
- `npm run lint`: passed
- Playwright:
  - viewport: `1600x900`
  - path: 登录 -> 成本控制模块 A -> 配置向导第 5 步模块设置 -> 点击 `操作日志` -> 点击 `操作时间`
  - result: 右侧立即切到 `操作时间` 字段配置；无新的 console error
  - screenshots:
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-detail-workbench-compact.png`
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-detail-workbench-compact-v2.png`

## Result Notes
- 这轮问题不是单个控件样式，而是“延迟选中 + 容器高度策略”叠加造成的手感差和留白。
- 对这种高频配置区，后续优先从交互链路和高度分配下手，再做视觉细修。

## 2026-03-21 模块设置明细留白二次修正

### Requirement Spec
- 目标：继续收掉第 5 步单表场景里“明细页签配置”状态下的空白感，尤其是下半区表格画布看起来缩在上面、下面像空着的问题。
- 影响范围：`src/components/Dashboard.tsx` 里的 `renderTableBuilder`、明细页签切换与字段选中链路。
- 关键约束：
  - 不能只继续压缩高度，必须让紧凑画布真实吃满父容器。
  - 点击页签、列头、整表切换要保持即时反馈。
  - 空态不能只是大片白底，要像真实表格工作面。

### Checklist
- [x] 把紧凑画布从 `min-height` 改成真正 `h-full / flex-1` 占满父容器。
- [x] 把明细空态改成分布式骨架行，避免内容堆在上半截。
- [x] 用 transition 优化页签和列点击的状态切换。
- [x] 跑 `npm run lint` 并用浏览器复现 `操作日志 -> 操作时间`。

### Verification
- `npm run lint`: passed
- Playwright:
  - path: 登录 -> 成本控制模块 A -> 配置向导第 5 步 -> 操作日志 -> 操作时间
  - result:
    - 右侧立即切到 `操作时间` 字段配置
    - 下半区画布不再缩成上方一小块，骨架行沿整个表体分布
    - console `error` = `0`
  - screenshots:
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-operation-log-stage-tightened.png`
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-operation-log-stage-distributed.png`

### Result Notes
- 这次确认“看起来留白很多”不一定是外层舞台高度，明细紧凑画布如果只设 `min-height`，视觉上也会像缩到上面。
- 高频配置区的空态不能只放一个居中提示，要让表体本身有密度和节奏，否则再高的容器也会显得空。

## 2026-03-22 明细配置入口收敛与切换性能修正

### Requirement Spec
- 目标：严格收掉模块设置第 5 步里明细区的冗余视觉和冗余配置入口，让员工在明细表配置里一眼就能配 SQL、列和关联条件。
- 目标：把列点击、整表点击、页签切换的“轻微延迟”继续压到接近瞬时反馈。
- 影响范围：`src/components/Dashboard.tsx` 里的 `renderTableBuilder`、明细数据源配置区、以及模块设置检查器选中链路。
- 关键约束：
  - 去掉表格前面的骨架光影，不要再出现多余的几排装饰线。
  - 明细数据源只保留一处配置，不再分成“页签行为/去数据源配置”和“数据源卡片”两套路径。
  - 若填写模块编号，只做“自动带出模块主 SQL/默认条件”的增强，不改变正常 SQL + 列配置主路径。
  - 不能为了流畅度牺牲配置语义或引入新的视觉噪音。

### Checklist
- [x] 去掉表格画布左上角的骨架光影，保留干净表体。
- [x] 合并明细数据源配置，只保留 SQL、列翻译、关联条件和模块编号增强。
- [x] 移除导致切换延迟的状态调度，优化列/整表/页签切换到即时反馈。
- [x] 跑 `npm run lint` 并用浏览器复现 `操作日志 -> 列点击 / 整表点击 / 数据源配置`。

### Progress Summary
- 明细页签点击默认不再落到单独的页签行为面板，而是直接切到当前明细整表配置，右侧第一屏就是数据源、SQL、关联条件和列翻译。
- 明细数据源入口收敛成一条主路径：模块编号只作为增强项，填入后自动带出对应模块主 SQL，并保留一键带入当前模块主表列。
- 明细空画布去掉了几排装饰性的骨架光影，只保留干净画布和中心操作提示；同时把上下默认比例继续上提，收掉下半区发空。
- 选中链路补了同目标短路，列点击、整表点击和页签切换不再重复触发整轮检查器重渲。

### Verification
- `npm run lint`: passed
- Playwright:
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> 登录 -> 点击 `操作日志`
  - 结果：
    - 右侧默认直接显示 `明细数据源 / 明细 SQL 配置 / 关联条件 / 字段标识`
    - 点击模块编号快捷项 `FM-CO-001` 后，模块摘要出现，`主 SQL` 自动切为 `SELECT * FROM customer_archive`
    - 点击 `操作时间` 列后，右侧立即切到列配置；再点明细画布后，立即切回 `明细表配置 · 操作日志`
    - 当前浏览器控制台 `error` 为 `0`
  - 切换测量：
    - 列点击到右侧标题切换约 `216ms`
    - 整表点击到右侧标题切换约 `246ms`
  - 截图：
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-source-unified-no-skeleton.png`
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-source-module-autofill.png`

### Result Notes
- 对这种重度配置区，`页签本身` 不该先把用户带进次级行为配置；默认入口必须直达“当前对象的主配置”，否则点击会被感知成慢和绕。
- 装饰性的 skeleton 光影会被用户直接视为噪音，不应该再拿来填补空白；空白问题应通过高度分配和真实配置结构解决。
## 2026-03-22 明细类型入口位置回归

### Requirement Spec
- 目标：把明细工作台里的类型切换入口恢复到右下角，延续用户已经形成的理解路径。
- 影响范围：`src/components/Dashboard.tsx` 里的 `renderDocumentDetailWorkbench`。
- 关键约束：
  - 不能重新引入顶部双入口或额外说明文字。
  - 右下角入口要悬浮放置，不能重新吃掉工作台有效高度。
  - 保持当前明细数据源仍是一套统一配置逻辑。
- 不做内容：
  - 不重做右侧检查器数据结构。
  - 不回退前面已经完成的明细 SQL / 模块编号自动带入逻辑。

### Checklist
- [x] 把明细类型切换按钮从顶部工具栏移除。
- [x] 在明细工作台右下角恢复悬浮式类型切换入口。
- [x] 跑 `npm run lint` 并用浏览器确认位置、切换与布局都正常。

### Progress Summary
- 已把顶部工具栏收回到“只放页签”，类型切换重新做成右下角悬浮入口，不再占上方理解路径。
- 保留了统一后的明细数据源配置逻辑，没有回退到两套配置入口。
- 右下角类型入口已实测可切换 `表格 / 树表格 / 图表 / 网页`，同时不会重新拉高明细区。

### Verification
- `npm run lint`：通过
- Playwright：
  - 路径：登录 -> 成本控制模块 A -> 配置向导第 5 步 -> 点击 `操作日志` -> 点击明细画布
  - 结果：
    - 顶部只剩明细页签，不再重复出现类型按钮
    - 类型入口恢复到明细工作台右下角
    - 点击 `树表格` 后，右侧标题切换为 `明细树表格配置 · 操作日志`
    - 再点回 `表格` 后，右侧恢复为 `明细表配置 · 操作日志`
    - 浏览器控制台 `error` 为 `0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-filltype-bottom-right.png`

### Result Notes
- 对这类已经形成固定心智的高频入口，正确做法不是继续追求“上方统一”，而是保留历史位置并把它做成不占高度的悬浮控件。
## 2026-03-22 模块配置默认非全屏与顶部统一条件条

### Requirement Spec
- 目标：模块设置与限制措施默认进入时都保持非全屏，由用户自行决定是否展开。
- 目标：把基础档案主表条件和左侧条件收敛到模块工作区顶部，用一个切换按钮在 `主条件 / 左条件` 之间切换。
- 影响范围：`src/components/Dashboard.tsx` 中配置向导打开逻辑、模块设置 document 模式布局、条件条交互。
- 关键约束：
  - 不能再有进入第 5 步或第 6 步时自动 `setIsFullscreenConfig(true)` 的路径。
  - 顶部条件条必须替代主表和左表各自的条件区，不能保留两套重复入口。
  - 原最右侧 `主题` 按钮移除，改成条件作用域切换按钮；有左表时可切 `左条件`，否则显示 `主条件`。
  - 顶部条件条要保持高频配置顺手，主表与左表列区域下方布局不能被挤坏。
- 不做内容：
  - 不重做右侧检查器字段结构。
  - 不恢复主题按钮入口。

### Checklist
- [x] 去掉模块设置与限制措施进入时的默认全屏。
- [x] 提炼主表/左表共用的顶部条件条，并支持 `主条件 / 左条件` 切换。
- [x] 移除原主表右侧 `主题` 按钮与左右分散条件条。
- [x] 跑 `npm run lint` 并在浏览器里验证默认非全屏、条件切换和主左对齐。

### Progress Summary
- 已把默认全屏根因一起收掉：初始状态改为非全屏，新增模块和已有模块“配置”入口都会显式回到非全屏，模块设置/限制措施首屏也不再被 `useEffect` 强制拉成全屏。
- 文档模式下主表条件和左表条件已统一收到模块顶部，只保留一条条件带；主表区和左表区原先分散的条件条已移除。
- 原最右侧 `主题` 按钮已去掉，改成条件作用域切换位；当前无左树表时不显示切换按钮，有左树表场景会切 `主条件 / 左条件`。

### Verification
- `npm run lint`：通过
- Playwright：
  - 路径 1：登录 -> 成本控制模块 A -> 配置向导第 5 步
  - 结果：
    - 底部按钮显示 `全屏配置`，不是 `退出全屏`
    - 页面中已无 `主题` 文案
    - 顶部只保留一条条件带，没有主表/左表两套重复条件区
  - 路径 2：切到第 6 步 `限制措施`
  - 结果：
    - 页面中仍显示 `全屏配置`，默认非全屏状态保持一致
    - 页面中无 `主题` 文案
  - DOM 结果：
    - step5：`hasThemeButton=false`、`hasFullscreenExit=false`、`hasFullscreenEnter=true`
    - step6：`hasThemeButton=false`、`hasFullscreenExit=false`、`hasFullscreenEnter=true`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-top-condition-toolbar-nonfullscreen.png`

### Result Notes
- 这次不是单纯把按钮换个位置，而是把“默认全屏”和“条件条分散”两个根因一起消掉了；否则用户下一次从别的入口进来还是会被旧逻辑覆盖。
- 当前示例模块没有树形节点关联字段，所以浏览器里只能实测到 `主条件` 顶部条；`左条件` 切换走的是代码路径和状态同步逻辑，待有左树表场景时可继续补一张实机截图。

## 2026-03-22 Shadcn UI 适配评估

### Requirement Spec
- 目标：评估 `shadcn/ui` 是否适合当前项目未来的工业级办公场景，并和现有自定义工作台风格做差异对比。
- 影响范围：当前前端技术栈、现有 Dashboard 视觉体系、后续拖拽配置工作台的组件化方向。
- 关键约束：
  - 本轮不改业务代码，只做架构与 UI 方向判断。
  - 结论要区分“界面风格更适合”与“拖拽交互更丝滑”这两类问题，不能混为一谈。
  - 需要结合当前项目实际栈和官方资料，而不是只做抽象判断。
- 不做内容：
  - 不直接引入 `shadcn/ui`。
  - 不输出迁移代码或改造 PR。

### Checklist
- [x] 查看当前项目 UI 栈与自定义视觉体系。
- [x] 查阅 `shadcn/ui` 官方文档与组件形态。
- [x] 对比当前风格与 `shadcn/ui` 在工业办公场景下的差异。
- [x] 给出适合当前项目的落地建议与风险判断。

### Progress Summary
- 当前项目已经是 `React + Vite + Tailwind CSS 4`，技术上接入 `shadcn/ui` 的阻力不高，但现有界面大量依赖 `cloudy-glass-*` 这类自定义玻璃化工作台样式。
- `shadcn/ui` 更适合做克制、稳定、密集、可维护的后台工作台壳层，尤其适合表单、Tabs、Sheet、Dialog、Sidebar、Table、ScrollArea 这些办公配置区。
- 它不会自动解决“拖拽设计器更丝滑”的问题；拖动、吸附、重排、resizable 等核心体验仍取决于当前状态流、渲染链路和专门的 DnD/布局方案。

### Verification
- 本地代码核对：
  - `package.json` 已确认当前项目使用 `React 19 / Vite 6 / Tailwind CSS 4`
  - `src/components/Dashboard.tsx`、`src/index.css` 已确认当前项目主要依赖自定义工作台样式体系
- 官方资料核对：
  - `https://ui.shadcn.com/docs`
  - `https://ui.shadcn.com/docs/components/data-table`
  - `https://ui.shadcn.com/docs/components/sidebar`

### Result Notes
- 结论：适合迁，但更适合先迁“工作台壳层和基础组件语法”，不适合把拖拽设计器核心能力寄希望于 `shadcn/ui` 本身。
- 如果目标是工业级办公平台，`shadcn/ui` 相比当前玻璃化、自定义感很强的视觉体系，会更克制、更耐用、更适合员工长时间高频配置。
- 最合理路径是混合方案：保留当前设计器核心与高频拖拽逻辑，先把外围的 Tabs、表单、检查器、弹层、列表、工具栏、侧栏收敛到 `shadcn/ui` 风格，再决定是否继续深迁。

## 2026-03-22 模块设置交互与非全屏布局修正

### Requirement Spec
- 目标：优化模块设置第 5 步的高频配置体验，让列宽拖拽更自由、点击条件/左表不再制造下方留白、非全屏下右侧配置区仍保持足够宽度。
- 影响范围：`src/components/Dashboard.tsx` 中表格列宽拖拽逻辑、文档模式上下 pane 布局、条件/左表切换后的工作区高度策略、右侧检查器宽度计算。
- 关键约束：
  - 列宽允许被拖到接近 `0px`，不能再被标题文案宽度卡住。
  - 明细点击默认数据源配置逻辑保持不回退，本轮重点只修交互和布局。
  - 非全屏下右侧页签和标题不能因为面板过窄而换行，宽度策略要接近全屏时的可读性。
  - 不能用简单隐藏内容的方式掩盖留白，必须修正 pane 分配与空态画布占满策略。
- 不做内容：
  - 不重做限制措施页。
  - 不引入新的 UI 库或重构整个配置向导骨架。

### Checklist
- [x] 修正列宽拖拽的最小宽度限制，支持拖到接近 `0px`。
- [x] 检查并修正条件点击、左表点击后的上下布局与空态占位，消除下方留白。
- [x] 调整非全屏下右侧检查器宽度策略，确保页签文案完整显示。
- [x] 跑 `npm run lint`，并按真实浏览器窗口验证拖拽、点击切换和非全屏布局。

### Progress Summary
- 表格列宽现在把“自动适配推荐宽度”和“拖拽最小宽度”拆开了：自动适配仍按内容估算，手动拖拽允许压到接近 `0px`，并通过 `table-layout: fixed` 消除标题文案反向撑宽的问题。
- 非全屏模式下右侧检查器不再沿用过窄的 `388px` 固定宽度，而是跟随可用空间扩大到接近全屏时的阅读宽度，同时页签按钮改成 `nowrap`，避免换行。
- 模块设置第 5 步的舞台重新恢复为 `flex-1` 占满内容区，非全屏不再把白边挪到舞台外；文档模式对主表/左表/条件这种上半区操作，还会自动抬高上半区有效高度，减轻下半区空带感。

### Verification
- `npm run lint`：通过
- Playwright：
  - 路径：登录 -> 成本控制模块 A -> 配置向导第 5 步
  - 列宽验证：
    - 打开 `操作日志` 明细
    - 拖拽 `操作时间` 列右边缘到最左
    - 结果：列宽由 `138px` 收到 `5px`
  - 非全屏布局验证：
    - 在默认非全屏下测量模块设置舞台到底部操作栏的 gap
    - 结果：gap 为 `36px`，不再出现舞台外大片空白
  - 右侧检查器验证：
    - `核心配置 / 扩展配置` 页签按钮保持单行 `nowrap`
    - 按钮高度为 `40px`，未出现换行撑高
  - 截图：
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/module-setting-column-collapse-and-inspector-width.png`

### Result Notes
- 这轮确认列宽“拖不窄”不是单纯拖拽函数的问题，而是 `min-width` 常量和表格自动布局一起把列标题变成了隐性下限；修正必须同时落在渲染宽度和 table layout 上。
- 非全屏白边的根因也不是单个面板高度，而是上一轮把舞台从 `flex-1` 拿掉后，空白从舞台内部转移到了舞台外；这次恢复舞台占满后，整体才重新顺下来。
- 当前默认示例模块没有树形节点关联字段，所以 Playwright 没法实机点到“左表可见”的场景；不过左表与主表/条件共享的是同一套上半区高度和非全屏舞台策略，代码路径已一并收敛。
## 2026-03-22 明细类型入口底部 Panel 收口

### Requirement Spec
- 目标：把明细工作台底部的类型切换从画布里的悬浮控件收成独立底部 panel，减少割裂感，并向 shadcn 风格的 segmented control 靠拢。
- 影响范围：`src/components/Dashboard.tsx` 中 `renderDocumentDetailWorkbench` 的底部布局、明细类型切换入口和工作台结构。
- 关键约束：
  - 保留“类型切换放在下方”的历史理解路径，不回到顶部工具栏。
  - 底部 panel 必须是工作台结构的一部分，不能再漂浮覆盖在画布上。
  - 本轮只做局部 shadcn 风格收口，不直接引入整套 `shadcn/ui` 组件库。
  - 不能破坏当前 `表格 / 树表格 / 图表 / 网页` 的切换逻辑。
- 不做什么：
  - 不重做右侧检查器结构。
  - 不迁移全局设计系统或批量替换现有组件。

### Checklist
- [x] 调整明细工作台结构，拆出底部独立类型 panel。
- [x] 把类型切换按钮改成更接近 shadcn 的 segmented 控制样式。
- [x] 清掉旧的右下角悬浮定位，避免画布内割裂。
- [x] 运行 `npm run lint`，并用浏览器验证位置、样式和切换正常。

### Progress Summary
- 明细工作台底部的类型入口已经从 `absolute` 悬浮块改成了工作台自带的底部栏，结构上和画布分离，不再贴在内容层上。
- 类型切换按钮改成了更接近 shadcn 的 segmented 控制语法：中性底色、独立内层容器、激活项用轻量 elevation 表示，不再靠蓝色悬浮 pill 撑视觉。
- 面板左侧保留了最小摘要，只显示当前明细类型和图标，避免再加解释性文字。

### Verification
- `npm run lint`：通过
- Playwright：
  - 视口：`1600x900`
  - 路径：登录 -> 成本控制模块 A -> 配置向导第 5 步 -> 点击 `操作日志`
  - 结果：
    - 明细类型切换已位于明细工作台底部独立 panel 中
    - 点击 `树表格` 后，右侧标题切换为 `明细树表格配置 · 操作日志`
    - 控制台 `error` 为 `0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-filltype-bottom-panel-shadcn-like.png`

### Result Notes
- 这轮确认“放在下面”不等于“继续悬浮在画布里”，高频模式切换入口一旦决定下沉，就应该并入工作台底部结构本身。
- 在未整库迁移 `shadcn/ui` 之前，先把高频入口改成更克制的 segmented/panel 语法，是风险最小的落地方向。
## 2026-03-22 条件工作台与明细底部 Panel 收口

### Requirement Spec
- 目标：把模块设置顶部条件区改成更接近 `shadcn/ui` 的工作台语法，减少旧式玻璃工具条噪音。
- 目标：条件控件总宽度固定为 `175px`，不再允许按名字长度动态撑开。
- 目标：点击顶部条件面板本身即可在右侧进入“条件总览配置”，集中设置行数、分栏数和批量粘贴构建。
- 目标：明细底部类型切换保留当前结构，但选中色回到用户熟悉的旧蓝色，并去掉左侧冗余介绍。
- 影响范围：`src/components/Dashboard.tsx` 里的 document 模式顶部条件条、右侧检查器分支、明细底部类型 panel。
- 关键约束：
  - 条件区不再沿用旧的动态宽度、拖拽调宽和说明式工具条。
  - 每个条件控件总宽度固定 `175px`，包含名字与输入壳层。
  - 面板总览配置需要支持设置控件行数、分栏数，以及粘贴 Excel/列名批量生成条件。
  - 左表存在时，顶部条件总览需要能切换 `主条件 / 左条件`，并沿用各自数据源。
  - 本轮只做 `shadcn/ui` 风格迁移，不强制整库安装 `shadcn/ui` 组件包。
- 不做内容：
  - 不改限制措施页。
  - 不重做明细 SQL / 模块编号的数据语义。

### Checklist
- [x] 更新顶部条件工作台：固定 175px、去掉旧拖拽宽度和冗余说明。
- [x] 新增条件总览配置入口，支持行数、分栏数、批量粘贴构建。
- [x] 保持主条件 / 左条件切换，并让右侧检查器可编辑总览参数。
- [x] 收口明细底部类型 panel：恢复旧蓝色选中色，去掉左侧介绍。
- [x] 运行 `npm run lint`，并用浏览器验证条件总览、批量构建、底部类型切换和控制台无报错。

### Progress Summary
- 顶部条件区已经从旧 `renderDocumentGridToolbar` 变体切到新的 workbench：面板整体可点选，主/左条件共用同一套 shadcn 风格工作台语法。
- 条件控件总宽度已经固定为 `175px`，名字和输入壳层一起收进统一卡片，不再跟着字段名长短变化。
- 右侧检查器新增了“条件总览”分支，可直接配置行数、分栏数，并粘贴 Excel / 列名批量构建条件。
- 明细底部类型切换保留了底部独立 panel，但左侧介绍已去掉，激活态换回旧蓝色强调。

### Verification
- `npm run lint`：passed
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> 点击顶部条件面板
  - 结果：
    - 右侧切到 `主条件总览`
    - 行数可改为 `3`
    - 分栏可改为 `2`
    - 文本框粘贴 `审批状态 / 审批人 / 审批时间` 后点击 `追加`，顶部条件由 `3` 项变为 `6` 项
    - 底部类型 panel 文本中不再包含 `明细类型` 介绍文案
    - 控制台新增错误为 `0`，仅保留既有 `favicon.ico 404`
  - 截图：
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/condition-workbench-shadcn-and-detail-panel.png`

### Result Notes
- 这轮确认顶部条件区不能继续套旧工具条逻辑，否则布局级配置只能零散塞进单个条件表单里。
- 对高频配置区，`shadcn/ui` 更适合作为“信息语法”和“面板节奏”迁移目标，先把结构换掉比先装整库更有效。
- 当用户接受新结构但要求恢复旧颜色记忆时，正确做法是保留新结构，只回调激活态的视觉信号，而不是回退整个布局。
## 2026-03-22 条件条扁平化与分隔条稳定

### Requirement Spec
- 目标：明细下方选中表格或列时，中央分隔条与上下区域高度保持稳定，不再出现“点一下布局就动一下”的感受。
- 目标：顶部条件条改成更扁平的工作条语法，去掉明显卡片感，方便肉眼判断控件间距。
- 目标：条件名称与控件之间更靠近，同时恢复条件宽度拖拽；新建条件默认宽度仍从 `175px` 起步。
- 影响范围：`src/components/Dashboard.tsx` 中 document 模式的上下 pane 高度策略、顶部条件工作条、条件字段编辑区。
- 关键约束：
  - 不能再把条件宽度强制写死成不可变常量。
  - 不能回退顶部条件总览与批量构建能力。
  - 不能通过隐藏内容掩盖下半区留白，必须修正 pane 高度与工作台占满策略。
- 不做什么：
  - 不重做明细数据源语义。
  - 不改限制措施页。

### Checklist
- [x] 更新条件字段归一化与字段编辑逻辑：默认 `175px`，但保留实际宽度并可编辑。
- [x] 重构顶部条件条：收紧标签与控件间距、去掉卡片感、接回宽度拖拽与双击自适应。
- [x] 稳定 document 上下 pane 高度，避免选中明细表格/列时分隔条位置跳动。
- [x] 运行 `npm run lint` 并用浏览器实测条件拖宽、表格/列切换和布局稳定性。

### Progress Summary
- 条件字段恢复成“默认 175px，但宽度保留在字段数据里”的模式；右侧字段编辑区和顶部拖拽都能改宽度，不再被常量锁死。
- 顶部条件区已经从卡片块改成扁平行式 workbench：标签和控件更近，外层只保留轻量面板语法，方便判断间距。
- document 上下 pane 高度改成稳定策略，点击明细整表和列时不再根据选中目标切换 top pane 的最小高度。

### Verification
- `npm run lint`：通过。
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录`
  - 分隔条测量：点击明细整表画布，再点击 `附件名称` 列；中央分隔条 `y` 坐标从 `655` 到 `655` 再到 `655`，没有跳动。
  - 条件拖宽测量：首个条件行宽度从 `284px` 拖到 `349px`，拖拽与宽度回写都生效。
  - 控制台：`error = 0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-condition-flat-resize-stable.png`

### Result Notes
- 这次确认“条件默认宽度”和“条件是否可调”不能绑死在一起；默认值应稳定，实际宽度仍要交给用户拖拽和细调。
- 分隔条跳动的根因不是明细表格本身，而是 top pane 高度跟着选中范围切换；把高度策略改稳定后，点整表和点列都不再让大框架抖动。
- 对高频条件条，去掉卡片感比继续叠装饰更重要；用户需要先看清真实间距，再谈视觉强化。

## 2026-03-22 条件工作台按行拖放与顺滑调宽

### Requirement Spec
- 目标：移除顶部条件总览里的“每行分栏”概念，改成只保留“控件行数”，并让条件控件可以直接拖入任意一行。
- 目标：条件工作台不再依赖列式 grid 推导布局，而是按真实“行”组织，用户能直观看到每一行并进行排布。
- 目标：条件控件宽度拖拽更丝滑，减少拖动时的卡顿感。
- 影响范围：`src/components/Dashboard.tsx` 中 `ConditionWorkbenchConfig`、条件总览检查器、顶部条件工作台渲染、条件宽度拖拽更新链路。
- 关键约束：
  - 不能保留“每行分栏”输入并同时又做按行拖放，避免双重布局模型。
  - 行数仍然要由总览控制；新增和批量生成的条件要能落到可见行里。
  - 条件宽度默认值仍为 `175px`，但宽度可拖拽修改。
  - 交互要优先直接操作，不再依赖抽象数字配置去理解排布。
- 不做内容：
  - 不改明细数据源语义。
  - 不重做右侧检查器其他分支。

### Checklist
- [x] 更新任务数据结构：移除 `columns`，补齐条件的行归属信息。
- [x] 重构顶部条件工作台为按行渲染，并支持拖到任意一行。
- [x] 从右侧条件总览里移除“每行分栏”，保留行数与批量构建。
- [x] 优化条件宽度拖拽更新链路，降低拖拽卡顿。
- [x] 运行 `npm run lint` 并用浏览器验证按行拖放、调宽和控制台无新报错。

### Progress Summary
- 条件总览的布局模型已经从“行数 + 分栏数”收成“行数 + 直接拖放”，`ConditionWorkbenchConfig` 不再保留 `columns`，条件字段本身新增了行归属信息。
- 顶部条件工作台已经改成真实行容器：每一行都可以直接接收条件拖入，空行也会显示投放提示，不再依赖 grid 分栏去猜布局。
- 条件宽度拖拽不再每一帧把整个条件数组 `map` 一遍；条件区改成拖动时直接预览、松手再提交宽度，卡顿感明显减轻。

### Verification
- `npm run lint`：通过
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> 点击顶部条件面板
  - 结果：
    - 右侧“布局总览”只剩 `控件行数`，不再显示“每行分栏”
    - 把 `控件行数` 改成 `3` 后，顶部条件区出现 3 条真实行容器
    - 将 `客户编号` 从第 1 行拖入第 2 行后，顶部条件区立即重排，第二行显示该条件
    - 条件宽度拖拽实测从 `240px` 调到 `330px`
    - 控制台 `error = 0`
  - 截图：
    - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/condition-row-drag-and-resize.png`

### Result Notes
- 当用户明确要“拖入每一个控件行数中”时，正确方案不是继续叠加新的数字配置，而是让布局本身回到可见、可拖、可投放的真实行工作面。
- 条件宽度拖拽的顺滑度，核心在于减少高频状态写回；这次把拖动过程和最终提交拆开后，保留了实时视觉反馈，也避免了每一帧重刷整个条件条。

## 2026-03-22 右侧详情区 shadcn 化收口

### Requirement Spec
- 目标：整体优化模块配置里的右侧详情区，移除大量低价值说明文字，统一为更克制、更适合高频填写的 shadcn/workbench 风格。
- 目标：主表、列、条件、明细、右键、颜色这些右侧分支使用同一套壳层、卡片和输入控件语法，减少现在“一页一个味道”的问题。
- 目标：表单控件更适合工业级办公填写，优先保证清晰、好点、好填，而不是继续强调玻璃拟态。
- 影响范围：`src/components/Dashboard.tsx` 中 `renderColumnOperationPanel`、相关 manager/detail 区块、菜单信息步骤右侧详情壳层，以及限制措施右侧输入控件样式。
- 关键约束：
  - 本轮以现有业务逻辑不变为前提，重点改公共骨架、控件风格和冗余说明。
  - 不强制整库安装 `shadcn/ui`，但视觉和交互语法要明显向 shadcn 的后台表单靠拢。
  - 需要减少说明文字，但不能牺牲必要字段名、占位和状态信息。
- 不做内容：
  - 不重写右侧详情的数据结构。
  - 不重做模块设置和限制措施的业务语义。

### Checklist
- [ ] 更新任务文档与教训，明确右侧详情统一收口方向。
- [ ] 重构右侧详情公共壳层、tabs、卡片和输入控件样式。
- [ ] 删减右侧详情里的冗余说明文案和低价值占位提示。
- [ ] 覆盖主表/列/条件/明细/右键/颜色等分支，并同步收口菜单信息右侧详情。
- [ ] 运行 `npm run lint` 并用浏览器验证高频填写与切换是否正常。

### Progress Summary
- 进行中。

### Verification
- 待执行。

### Result Notes
- 待补充。

## 2026-03-22 右侧详情 shadcn 化结果补充

### Progress Summary
- 右侧检查器新增了可复用的 `shadcn-inspector` 基础语法，统一了 panel、header、badge、tabs、input、textarea 的视觉和交互节奏。
- `菜单信息`、`模块设置`、`限制措施` 这三类高频右侧详情都接到了同一套更克制的表单壳层，不再继续沿用旧的玻璃感大圆角说明卡。
- `主表/左表` 的“落表说明”从一排技术标签改成了更短的映射卡和动作入口；`限制措施` 里去掉了多余的提示摘要与段值说明，只保留可编辑信息。
- 冗余提示继续收口：字段标识解释、颜色规则长说明、限制措施里的说明型 hint 都被压掉或改成最短提示。

### Verification
- `npm run lint`：通过。
- Playwright 实测：
  - 路径一：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> `模块设置`
  - 结果一：右侧 `主表配置` 已切到更接近 shadcn 的单层 card + segmented tabs 语法；`落表映射` 不再是一串技术标签；控制台 `error = 0`
  - 截图一：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/module-setting-right-inspector-shadcn.png`
  - 路径二：同页 -> `限制措施`
  - 结果二：右侧详情去掉了多余提示块，表单与只读摘要分层更清楚；控制台 `error = 0`
  - 截图二：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/restriction-right-inspector-shadcn.png`
  - 路径三：同页 -> `菜单信息`
  - 结果三：右侧目录/概览/扩展区已经统一到同一套 panel 语法；控制台 `error = 0`
  - 截图三：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/menu-info-right-inspector-shadcn.png`

### Result Notes
- 这轮确认“全部用 shadcn UI 改一遍”在当前仓库里最稳的落地方式，是先抽一层统一的右侧检查器基础语法，再逐块迁移高频分支，而不是一次性整库替换。
- 右侧详情真正影响填写效率的不是单个输入框是否更圆，而是壳层节奏、说明密度、标签层级和技术信息呈现方式；这次已经先把这些高频噪音收掉。

## 2026-03-22 单据主表工作台与明细填充修正

### Requirement Spec
- 目标：把单据主表控件区收成和顶部条件区同一套 shadcn/workbench 语法，避免现在“条件是一套、单据主表又是一套”的割裂感。
- 目标：单据主表控件拖动和调宽要更丝滑，优先减少拖动过程中的状态抖动和视觉噪音。
- 目标：单据主表选中后的右侧详情，收成和条件详情同一层级与节奏，删掉多余说明，让填写更直接。
- 目标：修复单据主表点击下方明细后舞台整体缩一截的问题，让明细区域继续填满，不再留下明显空带。
- 影响范围：`src/components/Dashboard.tsx` 中单据头部看板、单据主表右侧检查器、单据主表/明细上下布局与相关拖拽链路。
- 关键约束：
  - 单据主表仍保留现有 WinForm 式拖拽能力和对齐吸附，不回退为普通列表表单。
  - 主表 UI 要向条件区靠拢，但不能引入卡片感过重的块状包装，避免干扰间距判断。
  - 明细填充问题要从容器高度和 pane 分配上修，不靠继续塞占位块掩盖。
- 不做内容：
  - 不改单据主表字段数据结构。
  - 不改明细类型本身的业务语义。

### Checklist
- [ ] 同步更新任务文档与教训，明确“单据主表与条件区统一语法、明细舞台填充”的目标。
- [ ] 重构单据主表控件预览与选中态，使其风格和顶部条件区一致。
- [ ] 优化单据主表拖拽/调宽链路，降低拖动过程的迟滞和抖动。
- [ ] 把单据主表选中后的右侧详情收成与条件一致的填写工作台。
- [ ] 修复单据主表点击明细后下半区缩短与留白问题。
- [ ] 运行 `npm run lint` 并用浏览器实测主表拖动、主表选中、点击明细后的填充效果。

### Progress Summary
- 进行中。

### Verification
- 待执行。

### Result Notes
- 待补充。

## 2026-03-22 单据主表工作台与明细填充修正（结果补充）

### Progress Summary
- 单据主表控件预览已经收成和顶部条件区一致的扁平 workbench 语法，去掉了偏卡片的旧控件壳层，标签宽度也压回更接近条件区的节奏。
- 单据主表拖拽与调宽链路改成了“拖动中只维护 live preview，松手后再提交字段数组”，避免鼠标移动时每一帧都全量重刷主表字段集合。
- 单据主表字段选中后的右侧详情，已经切成和条件区同一套检查器语法，摘要指标使用 `控件标识 / 控件类型 / 控件宽度 / 标签宽度`，基础卡标题也收成了 `控件定义`。
- 单据模式第 5 步舞台与单据工作台都重新吃满了父容器高度；点击下方明细后，下半区不再缩成一条并在底部留下大块空带。

### Verification
- `npm run lint`：通过。
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=table` -> `立即登录`
  - 主表字段验证：
    - 点击 `单据时间`
    - 结果：右侧标题切到 `单据时间 / 单据头部控件`，摘要卡显示 `控件标识 / 控件类型 / 控件宽度 / 标签宽度`
    - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/bill-header-selected-inspector.png`
  - 明细填充验证：
    - 点击下方 `点击配置单据明细表`
    - 结果：右侧切到 `单据明细 / 表格级配置`，下半区继续填满主工作台，没有再缩成一截
    - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/bill-detail-selected-fill.png`
  - 拖动验证：
    - 对 `单据时间` 执行实际拖拽
    - 结果：字段位置发生变化，说明拖拽链路已由实时预览承接
  - 控制台：
    - `error = 0`
    - 输出文件：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/bill-header-console-errors.txt`

### Result Notes
- 这轮确认单据主表的“丝滑感”问题不在动画参数，而在拖动过程中不该反复写回完整字段数组；把高频更新拆成 live preview 后，交互会稳定很多。
- 明细点击后“缩一截”的根因也不是空态组件本身，而是单据舞台和外层 stage 的高度链路没有同时吃满；把 `flex-1 / h-full / min-h-0` 在父子容器里一起收齐后，留白自然消失。

## 2026-03-22 单据主表流式布局与插入式拖放

### Requirement Spec
- 目标：把单据主表从当前的自由坐标拖动改成和基础档案条件区一致的 shadcn/workbench 流式布局。
- 目标：点击单据主表整体时，也像条件面板一样在右侧配置“行数”，不再依赖自由坐标排布。
- 目标：拖动单据主表控件到另一个控件前方时，目标控件及其后续控件自动顺延，形成真正的“插入控件”体验。
- 影响范围：`src/components/Dashboard.tsx` 中单据主表工作台、单据主表字段拖放模型、单据主表表格级配置的右侧检查器。
- 关键约束：
  - 单据主表控件视觉要统一到条件区同款扁平 shadcn 语法。
  - 单据主表不再支持自由拖动到任意坐标点，拖放语义改成“按行、按顺序”。
  - 调宽能力保留，但不应影响插入式拖放和流式补位。
  - 插入式拖放需要支持跨行，拖到目标控件前时自动后移。
- 不做内容：
  - 不改明细数据源语义。
  - 不重做单据头部字段本身的数据模型，只补布局相关元信息。

### Checklist
- [ ] 更新任务与教训，明确“自由拖动 -> 流式插入布局”的新规则。
- [ ] 为单据主表字段补充行数/顺序布局元信息，并提供右侧可编辑的主表行数配置。
- [ ] 把单据主表渲染改成条件区同款流式行容器。
- [ ] 实现单据主表控件拖到其他控件前的插入式重排。
- [ ] 保留并验证单据主表控件宽度调节。
- [ ] 运行 `npm run lint` 并用浏览器验证主表行布局、插入拖放和右侧行数配置。

### Progress Summary
- 进行中。

### Verification
- 待执行。

### Result Notes
- 待补充。

### Progress Summary（结果补充）
- 单据主表已经从自由坐标拖动改成按行流式工作台，头部字段和顶部条件区统一为同一套扁平 workbench 语法。
- 单据主表整表选中后的右侧检查器新增了“头部流式布局”区，支持直接设置控件行数，并明确显示“按行插入”的拖放语义。
- 单据主表字段支持插入式拖放：把字段拖到另一个字段前面时，目标字段和后续字段会自动顺位后移，而不是再做自由坐标覆盖。
- 单据主表字段宽度仍可拖动调整，但保留了旧的 live preview 链路，避免把整套调宽逻辑重写成高风险改动。

### Verification（结果补充）
- `npm run lint`：通过。
- Playwright 实测路径：`http://127.0.0.1:3000/?config=1&step=5&mode=table` -> `立即登录` -> 第 5 步 `模块设置`。
- 行数配置验证：
  - 右侧 `头部流式布局` 可直接编辑 `控件行数`。
  - 输入 `2` 后，单据主表字段被压回 2 行范围内；恢复为 `3` 后仍可继续通过拖放与整理调整。
- 插入式拖放验证：
  - 将 `操作人员` 拖到 `物料名称` 前。
  - 结果：`操作人员` 插入到第 1 行第 2 位，原来的 `物料名称` 和其后的字段顺位后移。
- 截图：
  - `E:/ai项目/LsSmartTool/LsAITool/output/playwright/bill-header-flow-layout.png`

### Result Notes（结果补充）
- 这轮确认单据主表不能继续沿用“老坐标系只换皮”的方式；一旦用户要“和条件区一样的样式 + 行布局”，就必须连布局模型一起切到按行插入。
- 对流式排布区，最稳定的改法是保留已验证过的宽度实时预览链路，只替换拖放语义；这样既能得到顺滑感，也不会把调宽功能一起打坏。

## 2026-03-22 基础档案条件插入式拖放

### Requirement Spec
- 目标：把基础档案顶部条件区的拖放语义同步成和单据主表一致的“插入式后移”。
- 目标：当条件控件拖到另一个条件前面时，目标条件及其后续条件自动往后移动一位，而不是只切换行或只做覆盖。
- 影响范围：`src/components/Dashboard.tsx` 里的 `renderDocumentConditionToolbar`、条件拖放状态和条件数组重排逻辑。
- 关键约束：
  - 保留现有按行工作台结构和行数配置，不回退成自由布局。
  - 保留跨行拖放能力；拖到空行仍然是追加到该行。
  - 不改条件业务字段语义，只改排布与拖放顺序。
- 不做什么：
  - 不改右侧条件总览表单结构。
  - 不改条件宽度拖拽逻辑。

### Checklist
- [ ] 更新任务文档与教训，记录条件区也需要插入式后移。
- [ ] 把条件拖放状态从“当前行”扩成“当前行 + 前插目标”。
- [ ] 实现条件拖到目标前方时的数组重排与顺位后移。
- [ ] 运行 `npm run lint` 并用浏览器验证条件前插拖放与控制台无新报错。
## 2026-03-22 基础档案条件插入式拖放（补充）
### Requirement Spec
- 目标：让基础档案顶部条件区和单据主表保持一致，拖动某个条件到另一条件前面时，目标条件与其后的条件自动后移一位。
- 影响范围：`src/components/Dashboard.tsx` 中的条件 workbench 拖放状态、条件重排逻辑和行内条件项渲染。
- 关键约束：
  - 保留现有按行 workbench 结构。
  - 保留跨行拖放能力，拖到空行仍然是落到该行末尾。
  - 不改条件字段业务语义，只改拖放排序规则。
- 不做什么：
  - 不改右侧条件总览表单结构。
  - 不改条件宽度拖拽逻辑。
- 如何验证成功：
  - `npm run lint` 通过。
  - 浏览器里把一个条件拖到另一个条件前面后，顺序发生前插重排，控制台无新错误。
### Checklist
- [ ] 更新任务文档与教训，记录条件区也要插入式拖放。
- [ ] 把条件拖放状态从“当前行”扩展成“当前行 + 前插目标控件”。
- [ ] 实现条件拖到目标前方时的数组重排与顺位后移。
- [ ] 运行 `npm run lint` 并用浏览器验证条件前插拖放与控制台无新报错。
### Progress Summary
- 进行中。
### Verification
- 待执行。
### Result Notes
- 待补充。
## 2026-03-22 基础档案条件插入式拖放（结果补充）
### Progress Summary
- 条件工作台拖放状态已经从“只记录行”改成“记录行 + 前插目标控件”，顶部条件区现在支持真正的前插重排。
- 拖到空行仍然是落到该行末尾；拖到某个条件前方时，会把目标条件及其后的条件自动顺位后移，不会破坏现有按行布局。
### Verification
- `npm run lint`：通过。
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录`
  - 操作：将 `测试日期` 拖到 `客户全称` 前。
  - 结果：顶部条件顺序从 `客户编号 / 客户全称 / 测试日期` 变为 `客户编号 / 测试日期 / 客户全称`。
  - DOM 顺序：`["客户编号","测试日期","客户全称"]`
  - 控制台：`error = 0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/document-condition-insert-drag.png`
### Result Notes
- 这轮确认条件区和单据主表必须保持同一套“插入式拖放”心智，否则用户会在两个高频排布区来回切换时出错。
## 2026-03-22 基础档案明细模块右侧详情重构

### Requirement Spec
- 目标：基础档案模式下，点击“明细模块”后，右侧详情改为参照 `p_systemdlltabdetail` 的配置面板。
- 影响范围：`src/components/Dashboard.tsx` 中 `selectedColumnContext.kind === 'detail-tab'` 的右侧检查器分支，以及对应的明细模块配置默认字段。
- 关键约束：
  - 右侧以 `p_systemdlltabdetail` 字段口径为主，不再只保留现有的 3 个轻量字段。
  - 去掉冗余说明文案，不做大段介绍，优先保证填写效率。
  - 沿用当前共享的 shadcn inspector 语法，不额外扩散到其他分支。
  - 不改动“明细整表配置”分支的语义，只改“明细模块”分支。
- 不做内容：
  - 不重构整套明细工作台。
  - 不改动单据模式明细配置逻辑。

### Checklist
- [x] 把本轮规则补充到 `tasks/lessons.md`。
- [x] 扩展 `buildDetailTabConfig`，补齐 `p_systemdlltabdetail` 对应的高频字段。
- [x] 将基础档案明细模块右侧详情改为精简的 shadcn 填写面板。
- [x] 修正“明细模块”点击入口，确保实际进入 `detail-tab` 分支。
- [x] 运行 `npm run lint` 并做浏览器点击验证。

### Progress Summary
- 已确认当前 `detail-tab` 分支只包含 `autoRefresh / disabled / disabledCondition`，与 `p_systemdlltabdetail` 差距过大。
- 已从《朗速详细设计平台V2-表结构设计文档》提取到 `p_systemdlltabdetail` 的核心字段，包括 `tab / tabKey / detailName / bandHeight / bandWidth / Fremark / unionFlag / gridDetailCheck / noColumnHeader / displayRows / dragcond / IsMrpDrag / mrpDragTag` 等。

### Verification
- `npm run lint`：通过
- Playwright：
  - 路径：`http://127.0.0.1:3000/?config=1&step=5&mode=document` -> `立即登录` -> 第 5 步 `模块设置` -> 点击基础档案下方 `关联附件`
  - 结果：
    - 右侧标题切到 `明细模块 · 关联附件`
    - 右侧显示 `p_systemdlltabdetail` 标识与 `基础定义 / 显示与关联 / 开关项`
    - 核心字段包含 `所属模块编号 / tabKey / 明细模块名 / 显示行数 / 列宽 / 行高 / 拖拽条件 / 备注`
    - 切到 `扩展配置` 后可编辑 `MRP 拖拽标记 / 禁用条件`
    - 把 `明细模块名` 从 `关联附件` 改为 `附件中心` 后，左侧页签名称同步更新
    - 控制台 `error = 0`
  - 截图：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-tab-p_systemdlltabdetail-inspector.png`
  - 控制台输出：`E:/ai项目/LsSmartTool/LsAITool/output/playwright/detail-tab-console-errors.txt`

### Result Notes
- 这轮确认“右侧分支做出来”不等于“用户能点到”。如果点击入口仍然落在 `detail-grid`，再完整的 `p_systemdlltabdetail` 表单也没有意义，所以本次一并修正了 `detail-tab` 的真实进入路径。
- 对“基础档案 -> 明细模块”这种高频填写面板，最稳的收法不是继续做说明卡，而是直接用共享的 shadcn inspector 骨架，把字段拆成 `基础定义 / 显示与关联 / 开关项 / 扩展配置` 这类短区块。
## 2026-03-22 明细页签与内部视图职责补充
### Requirement Spec
- 目标：把基础档案明细页签、明细表格视图、明细图表视图的右侧详情彻底拆开，避免切换底部类型时右侧自动串成另一套配置。
- 影响范围：`src/components/Dashboard.tsx` 中明细页签、明细表格、图表视图预留区点击以及右侧检查器 common/advanced 分支。
- 关键约束：
  - 明细页签只保留页签级属性，不重复承载数据来源、图表配置、表格摘要。
  - 点击表格区域才进入明细表配置；点击图表视图预留区才进入图表配置。
  - 图表右侧继续遵循 `p_systemdlltabchart` 字段口径，但布局需要收平、减少碎卡片和重复摘要。
  - 明细表配置继续和主表保持同构，数据来源只保留一套入口。
- 不做什么：不回退当前 shadcn/workbench 风格；不改限制措施；不重做底部类型切换入口。
### Checklist
- [x] 复核明细页签、明细表格、图表视图当前共用状态链路。
- [x] 让 `detail-grid` 区分“当前选中的是表格视图还是图表视图”，而不是只跟随底部类型自动切右侧。
- [x] 收口明细页签右侧，只保留页签级字段和开关。
- [x] 收口明细表格右侧，只保留数据来源、明细表属性、字段标识。
- [x] 收口明细图表右侧，只保留图表本身配置，并通过点击图表视图预留区进入。
- [x] 运行 `npm run lint` 并做浏览器真实点击验证。
### Verification
- `npm run lint`：通过。
- 浏览器验证：
  - 点击 `关联附件` 页签后，右侧进入 `明细模块 · 关联附件`，只保留页签级字段与开关。
  - 底部切到 `图表` 时，右侧不会自动跳图表配置。
  - 点击图表视图预留区后，右侧切到 `明细图表配置 · 关联附件`。
  - 再切回底部 `表格` 并点击表格区域后，右侧切回 `明细表配置 · 关联附件`。
  - 控制台 `error = 0`。
### Result Notes
- 这次不是继续压控件，而是把“页签对象”和“内部视图对象”的选中态分开了。这样底部类型切换只改变视图，不会再顺带把右侧检查器强行切到另一套详情。
- 图表配置改成了单独的图表视图工作流，数据来源和表格摘要不再重复塞进图表面板里。
