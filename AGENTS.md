# 项目协作规范

本文件用于约束本仓库内的协作方式、执行流程和交付标准。
若与更高优先级的系统、开发者或平台指令冲突，以高优先级指令为准；不冲突时，默认严格执行本文件。

## 1. 执行原则

- 简洁优先：所有改动尽量保持最小侵入，避免无意义扩散。
- 杜绝敷衍：必须定位根因，不接受仅掩盖现象的临时修补。
- 资深标准：交付前自问“资深工程师是否会认可这份方案和实现”。
- 结果导向：未证明功能正常前，不得宣称完成。

## 2. 规划优先

- 所有非简单任务必须先规划后执行。
- 非简单任务至少满足以下任一条件：
  - 需要 3 步以上操作。
  - 涉及架构、状态流、数据结构或交互方案取舍。
  - 需要构建、调试、验证、回归中的多个环节配合。
- 规划必须覆盖：
  - 需求规格澄清。
  - 具体执行步骤。
  - 风险点与假设。
  - 验证方式。
- 若执行出现明显偏差、阻塞或方案失真，必须先停下并重新规划，禁止强行推进。

## 3. 任务文档流程

- 每个非简单任务开始前，先更新 `tasks/todo.md`。
- `tasks/todo.md` 至少包含：
  - 需求规格。
  - 可核对的执行清单。
  - 进度标记。
  - 验证记录。
  - 结果复盘。
- 收到用户修正、反馈或否定后，必须同步更新 `tasks/lessons.md`。
- 启动本项目的新一轮复杂任务前，先回顾 `tasks/lessons.md`，避免重复犯错。

## 4. 子代理与上下文控制

- 对复杂问题，优先将调研、探索、并行分析、日志排查等工作进行隔离处理，保持主上下文简洁。
- 单个分析单元只解决一个问题，避免把多个目标混在一起。
- 若运行环境提供子代理能力，优先使用子代理。
- 若运行环境不提供独立子代理，则使用并行工具、独立步骤和分段摘要代替，目标不变。

## 5. 需求规格要求

- 开始实现前，先形成清晰的高层规格，减少歧义。
- 规格至少说明：
  - 用户目标。
  - 影响范围。
  - 关键约束。
  - 不做什么。
  - 如何验证成功。

## 6. 完成前强制验证

- 未验证成功，禁止标记任务完成。
- 验证方式按任务类型选择，包括但不限于：
  - 运行测试。
  - 类型检查。
  - 构建检查。
  - 日志检查。
  - 浏览器真实操作验证。
  - 与主干或修改前行为对比。
- 若无法执行某项验证，必须明确说明原因和剩余风险。

## 7. 优雅实现原则

- 对非简单改动，在动手前先判断是否存在更优雅、更统一、更可维护的实现方式。
- 若当前修复明显粗糙、重复、一次性或与现有结构冲突，应优先重写为更合理的方案。
- 简单、边界清晰且低风险的问题，可避免过度设计，但仍需保证代码质量。
- 交付前必须进行一次交叉校验，确认实现、样式、交互和验证结论彼此一致。

## 8. 缺陷修复策略

- 收到缺陷报告后，优先直接定位并修复，不要求用户重复拆解步骤。
- 主动查看日志、报错、失败测试、控制台输出和相关代码路径。
- 若发现 CI 或本地验证失败，默认应主动修复，不等待二次指令。

## 9. 输出要求

- 每一步变更只写高层摘要，不堆砌无意义细节。
- 对重要决策说明原因、权衡和验证结果。
- 完成后说明：
  - 改了什么。
  - 为什么这么改。
  - 如何验证。
  - 是否还有风险或待办。

## 10. Dashboard 专项规则

- 只要改动 `src/components/Dashboard.tsx` 或 `src/features/dashboard/**`，默认必须同时遵守：
  - [docs/dashboard-architecture-rules.md](/Users/apple/Desktop/未命名文件夹/LsSmartTool/LsAITool/docs/dashboard-architecture-rules.md)
- 对人和 AI 都一样适用，不允许例外地把新功能继续堆回 `Dashboard.tsx`。
- 如果需求涉及“表格详情配置 / 详情布局编辑”，默认必须优先复用统一能力：
  - `src/features/dashboard/detail-layout-designer`
  - `FieldBackedDetailLayoutDesigner`
  - 业务 adapter / bridge 模式
- 不允许再为某一个详情入口单独造一套新的拖拽布局编辑器。
- 如果确实必须突破该文档约束，提交前必须先在任务文档中明确写清原因、影响范围和回滚方式。
