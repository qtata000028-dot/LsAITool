export interface AuthSession {
  accessToken: string;
  companyKey: string;
  companyTitle: string;
  datasourceCode: string;
  departmentId: string;
  employeeId: number;
  employeeName: string;
  expiresAt: string;
  tokenType: string;
  tokenVersion: number;
  username: string;
}
